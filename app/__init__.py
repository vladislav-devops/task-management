"""
app — Core application layer for FastAPI Task Management API.

This package contains the foundational components required to run
the Task Management service: configuration, database setup, Redis
client integration, ORM models, and Pydantic schemas.

This module is intentionally empty; it serves as the package marker
so that ``app`` is recognised as a Python package.
"""
